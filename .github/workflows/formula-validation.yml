name: Formula Validation

on:
  push:
    branches: [main]
    paths:
      - 'Formula/**'
  pull_request:
    paths:
      - 'Formula/**'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  audit:
    name: Brew Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@master

      - name: Tap this repository
        run: brew tap slmingol/homebrew-tap "${{ github.workspace }}"

      - name: Audit formulas
        run: brew audit --strict slmingol/homebrew-tap/gauth slmingol/homebrew-tap/doxctl slmingol/homebrew-tap/gotcping slmingol/homebrew-tap/gorelease_ex

  test:
    name: Install & Test (${{ matrix.formula }}) / ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, macos-13]
        formula: [gauth, doxctl, gotcping, gorelease_ex]

    steps:
      - uses: actions/checkout@v4

      - name: Install formula
        run: brew install --verbose "${{ github.workspace }}/Formula/${{ matrix.formula }}.rb"

      - name: Run brew test
        run: brew test --verbose "${{ matrix.formula }}"

      - name: Verify binary in PATH
        run: command -v ${{ matrix.formula }}

      - name: Check for broken dependencies
        run: brew missing "${{ matrix.formula }}" || true
